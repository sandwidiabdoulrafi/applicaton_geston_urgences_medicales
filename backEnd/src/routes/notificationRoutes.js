// const express = require('express');
// const router = express.Router();
// const notificationController = require('../controllers/notificationController');

// // 📋 Récupérer les notifications (avec filtres optionnels)
// router.get("/get", notificationController.getNotif);

// // 🔄 Mettre à jour une notification
// router.put("/update", notificationController.updateNotif);

// // ✅ Marquer une notification comme lue
// router.patch("/:id/read", notificationController.markAsRead);

// // ✅ Marquer toutes les notifications comme lues
// router.patch("/read-all", notificationController.markAllAsRead);

// // ❌ Supprimer une notification
// router.delete("/delete", notificationController.deleteNotif);

// module.exports = router;